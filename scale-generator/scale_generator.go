// Package scale accepts a tonic note, intervals and constructs a scale
package scale

// Scale accepts a string representing the root note of a scale
//	and a string of intervals represented by m or M. Scale then
//	constructs a scale from this information
func Scale(tonic string, intervals []rune) (scale []string) {
	// create array of intervals from string
	return scale
}
